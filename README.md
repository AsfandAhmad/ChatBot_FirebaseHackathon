# Firebase Chatbot Project 🚀

## Setup
1. Clone repo: `git clone git@github.com:AsfandAhmad/ChatBot_FirebaseHackathon.git`
2. Install dependencies: `npm install`
3. Start dev server: `npm run dev`

## Deployment
`firebase deploy`
